# --- App ---
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.4

# --- Testing Core ---
pytest==8.2.2
pytest-asyncio==0.23.7

# --- API Testing ---
httpx==0.27.0          # FastAPI test client + async HTTP

# --- UI/E2E ---
playwright==1.44.0
pytest-playwright==0.5.0

# --- Reporting ---
allure-pytest==2.13.5

# --- Utilities ---
pytest-html==4.1.1     # Basit HTML raporu (Allure'a alternatif)
faker==25.8.0          # Fake data üretimi (JS'deki @faker-js gibi)
python-dotenv==1.0.1   # .env desteği
